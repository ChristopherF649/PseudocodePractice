-- This is the in-class pseudocode practice assignment -- 

// First Task //

string userInput;
string upperCaseText = "";
bool hasLowerCase = false;
bool invalidCharacter = false;

OUTPUT "Enter text (Letters only please): ";
INPUT uesrInput;

FOR each character 'c' in userInput
{
  IF c is a number OR c is a symbol
{
  invalid character = true;
}
ELSE IF c is a lowercase letter
{
  hasLowercase = true;
  c = convert c to uppercase
}
uppercaseText = upercaseText + c;
}

IF invalidCharacter == true
{
  OUTPUT "Error: Input must be letters only."
}
ELSE IF hasLowerCase == false
{
  OUTPUT "Input is already uppercase"
}
ELSE
{
  OUTPUT "text in all caps: " + upperCaseText;
}

//END

//Second Task

string inputOne;
string inputTwo;
float numberOne;
float numberTwo;
bool validInput = true;

OUTPUT "Enter first number: "
INPUT inputOne;

OUTPUT "Enter second number: "
INPUT inputTwo;

IF inputOne is NOT a valid number
{
  OUTPUT "Error: Please enter valid numbers only."
  validInput = false
}

IF inputTwo is NOT a valid number
{
  OUTPUT "Error: Second number is not valid";
  validInput == false;
}

IF validInput == true
{
  numberOne = convert inputOne to number
  numberTwo = convert inputTwo to number

  float sum = numberOne + numberTwo;

OUTPUT "The sun is: " + sum;
}

//END

// Third Task

string userInput;
float number;
float total;
bool isRunning = true;

OUTPUT "Enter the numbers you wish to add: ";
OUTPUT "Type "Done" to finish.";

WHILE isRunning == true;
{
  INPUT userInput;

  IF userInput == "done"
{
  running = false;
}
ELSE IF userInput is NOT a valid number
{
  OUTPUT "Error: please enter a valid number."
}
ELSE
{
  number = convert userInput to number;
   total = total+number;
}

OUTPUT "TOTAL SUM: " + total;
}

//END

// Fourth Task

string userInput;
int maxNumber;
int randomNumber;
bool valid = false;

WHIlE valid == false
{
  OUTPUT "Enter a maximum number (Greater than 0): "
  INPUT userInput

  IF userInput is not a valid int
{
  OUTPUT "Please enter a valid whole number."
}
Else
{
  maxNumber = convert userInput to int

  IF maxNumber = 0
{
   OUTPUT "Error: Number has to be greater than 0."
}
ELSE
{
  valid = true;
}

// use a random number generator
SEED random number

randomREsult = RANDOM number between 0 and maxNumber;

OUTPUT "Random number: " + randomResult;
}

//END

// Fifth Task

int number;

int leftCount = 0; // Odd
int rightCount = 0; // Even
bool running = true;

OUTPUT "Enter a random number: ";
OUTPUT "Type 'done' to stop";

While running == true
{
  INPUT userInput

  IF userInput == "done"
{
  running = false;
}
ELSE IF
{
  OUTPUT "Error: Please enter a valid number."
}
ELSE
{
  number = convert userInput to int;

  IF number %2 == 0
{
  OUTPUT "RIGHT";
  rightCount = rightCount + 1;
}

ELSE
{
  OUTPUT "LEFT";
  leftCount = leftCount + 1;
}

}

OUTPUT "LEFT" + leftCount + " Times";
OUTPUT "RIGHT" + rightCount + " Times";
}

//END
